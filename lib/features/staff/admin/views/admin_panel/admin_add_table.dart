import 'package:flutter/material.dart';

class AdminAddTable extends StatelessWidget {
  const AdminAddTable({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}