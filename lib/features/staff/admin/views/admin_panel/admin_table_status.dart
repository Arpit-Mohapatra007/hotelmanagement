import 'package:flutter/material.dart';

class AdminTableStatus extends StatelessWidget {
  const AdminTableStatus({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}