import 'package:flutter/material.dart';

class AdminUpdateDish extends StatelessWidget {
  const AdminUpdateDish({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}