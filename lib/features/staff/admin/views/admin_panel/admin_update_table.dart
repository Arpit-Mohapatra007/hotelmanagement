import 'package:flutter/material.dart';

class AdminUpdateTable extends StatelessWidget {
  const AdminUpdateTable({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}